{
  "name": "slides-to-pdf",
  "version": "1.0.0",
  "description": "Figma plugin to export slides as PDF with merge, compress, and reorder options",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "typecheck": "tsc --noEmit",
    "test": "node test/dist-current.test.js && node build.js && node test/selection.test.js && node test/export.test.js",
    "bench": "node --expose-gc --max-old-space-size=4096 test/merge.bench.js",
    "icons": "node scripts/gen-icons.js",
    "preview": "node test/preview.js"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.90.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
