node_modules/
*.js.map
.env
preview-shots/

# Publishing assets, regenerated by scripts/render-assets.js.
# Uploaded through Figma's publish dialog rather than read from the repo,
# so there is nothing to gain from committing a build artifact.
assets/*.png
